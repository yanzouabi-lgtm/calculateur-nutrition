{
  "name": "calculateur-nutrition-import",
  "version": "1.0.0",
  "type": "module",
  "description": "Scripts CLI : scraping Zooplus, publication OPFF, import Firebase",
  "scripts": {
    "dry-run":  "node run-import.js --dry-run --max 5",
    "import":   "node run-import.js",
    "opff-only": "node import-petfoodfacts.js --dry-run --max-pages=1"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "dotenv": "^16.0.0",
    "firebase-admin": "^12.0.0",
    "playwright": "^1.44.0"
  }
}
